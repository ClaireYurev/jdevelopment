package com.claireyurev;

public class Array {
    public Array(int size){
        int arraySize = size;
        createArray(arraySize);
        public void insert(int item){
            int newArrayMember = item;
            currentArray.[0] = newArrayMember;
        }
    }
    public int[] createArray(int arraySizeGiven){
        int currentLength = arraySizeGiven;
        int[] currentArray = new int[currentLength];
    }

}
